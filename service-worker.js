// service-worker.js
self.addEventListener('install', (event) => {
    // Código para la instalación del Service Worker
});

self.addEventListener('activate', (event) => {
    // Código para la activación del Service Worker
});

self.addEventListener('fetch', (event) => {
    // Código para gestionar solicitudes de red (fetch)
});
